import { BookDescriptionDirective } from './book-description.directive';
import { TestBed } from '@angular/core/testing';

describe('BookDescriptionDirective', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [BookDescriptionDirective]
    });
  });

  it('should create an instance', () => {
    // const directive = new BookDescriptionDirective(el);
    // expect(directive).toBeTruthy();
  });
});
